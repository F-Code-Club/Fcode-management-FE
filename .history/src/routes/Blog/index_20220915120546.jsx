import * as Styled from './Blog.styled';

const Blog = () => {
    return (
        <Styled.Background>
            <Styled.Wrapper></Styled.Wrapper>
        </Styled.Background>
    );
};

Blog.propTypes = {};

export default Blog;
